# fwOS Phase 2 static default; the Phase 3 config engine replaces this file.
# LAN-only reachability is enforced by /etc/nftables.conf (WAN input drops).
PermitRootLogin yes
PasswordAuthentication yes
